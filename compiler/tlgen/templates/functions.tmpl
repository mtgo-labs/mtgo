// Code generated by tlgen. DO NOT EDIT.

package {{.PkgPrefix}}

import (
	"bytes"

	"github.com/pageton/mtgo/tg"
)
{{range .Types}}

// {{.Name}} represents the RPC function {{.QualName}}#{{printf "%08x" .ID}}.
//
// See https://core.telegram.org/method/{{.QualName}} for reference.
type {{.Name}} struct {
	{{- range .Fields}}
	{{.Name}} {{.GoType}}
	{{- end}}
}

// New{{.Name}} creates a new {{.Name}} initialized with the provided field values.
func New{{.Name}}({{range .Fields}}{{if not .IsFlags}}{{.Name}} {{.GoType}}, {{end}}{{end}}) *{{.Name}} {
	return &{{.Name}}{
		{{- range .Fields}}
		{{- if not .IsFlags}}
		{{.Name}}: {{.Name}},
		{{- end}}
		{{- end}}
	}
}

// ConstructorID returns the TL constructor identifier 0x{{printf "%08x" .ID}} for {{.Name}}.
func (v *{{.Name}}) ConstructorID() uint32 {
	return 0x{{printf "%08x" .ID}}
}

// Encode serializes {{.Name}} to a bytes.Buffer using the TL binary protocol.
func (v *{{.Name}}) Encode(b *bytes.Buffer) error {
	tg.WriteInt(b, 0x{{printf "%08x" .ID}})
	{{- range .WriteLines}}
	{{- if .IsGuarded}}
	if v.{{.FlagName}}&(1<<{{.FlagBit}}) != 0 {
		{{.Code}}
	}
	{{- else}}
	{{.Code}}
	{{- end}}
	{{- end}}
	return nil
}
{{end}}
